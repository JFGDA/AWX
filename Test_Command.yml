- hosts: all
  ignore_errors: yes
  tasks:
   - name: Backup
     ios_command:
       commands: show run 
     register: output
#       provider: "{{cli}}"
   - debug: 
           var: output.stdout_lines
